import baseRules from '../../../formRules/base.js';

let list = {
  mobile:[baseRules.mobile],//手机号码
  name: [baseRules.inputLen(0, 50), baseRules.illegalChar()],//账号
}
let add = {
  account: [baseRules.requiredNoEvent,baseRules.inputLen(0, 50), baseRules.illegalChar()],//账号
}

export {
  list,
  add
}
